void bubbleSort(vector<int>& arr, int n)
{   
    // Write your code here.
    int k=n-1;
    for(int i=0;i<n;i++){
        for(int j=0;j<k;j++){
            if(arr[j]>arr[j+1]){
                swap(arr[j],arr[j+1]);
            }
            
        }
        k--;
    }
    
}
